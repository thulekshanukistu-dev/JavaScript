
//Q1
function findElementIndex(arr, targetElement) {
        for (let i = 0; i < arr.length; i++) {
            if (arr[i] == targetElement) {
                sum += num[i];
            }
        }
          console.log("Total "+sum);  
    }
    findElementIndex(arr,targetElement);

    //Q2 
    function findElementIndex(arr,targetElement){

    }








    //