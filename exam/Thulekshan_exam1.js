//01.body mass index (BMI) is a measure of health based on the weight and height//


// a .convert the wight from pounds to killograms//

function calculate(convertwight){
 for(let i =0; pound=0.453592; j++ )
    return a ;
}


// b.convert the height from centimeter to metet//

function calculate(converCMtoM){
    for(let i =0; 1=100 ; j++)
        return a;

}

// c.measure thhe bmi value by given both height and weight via parameters//

function calculate(bmicalculate){
    for(let i =0;weight/height*height;j++)
        return a,b;

}


// d.display the messages based on the calculated index values//
 function a(calculateindex){
    switch  (  let i=0; bmi<=18.5;j++){
       "print under weight "
    }
    switch ( let i=0; bmi<=24.9;j++){
         "print normal"
    }
    
     switch (let i=0;bmi<==29.9;j++)  {
      
     }
     switch (let i=0;bmi<==30;j++){
       
     }
 }
//e. main
console.log(calculate(convertwight));
 console.log(calculate(converCMtoM));
 console.log(calculate(bmicalculate))
 console.log(a(calculateindex))


 // 02.recursive function to return the fibonacci value of the given number//
 var num =2 
 function calculate(number){
     for(let i = 0; num/2; j++){
        
     }
        
    
 }
 console.log(calculate(number));

